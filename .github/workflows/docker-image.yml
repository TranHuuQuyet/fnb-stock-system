name: CI and Docker Images

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-image-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: tranhuuquyet

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci

      - name: Generate Prisma client
        working-directory: ./backend
        run: npm run prisma:generate

      - name: Run backend tests
        working-directory: ./backend
        run: npm test -- --runInBand

      - name: Build backend
        working-directory: ./backend
        run: npm run build

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Run frontend tests
        working-directory: ./frontend
        run: npm test

      - name: Build frontend
        working-directory: ./frontend
        env:
          NEXT_PUBLIC_API_BASE_URL: /api/v1
        run: npm run build

      - name: Prepare production env files
        shell: bash
        run: |
          cat <<'EOF' > .env.production.compose
          COMPOSE_PROJECT_NAME=fnbstore
          APP_DOMAIN=fnbstore.store
          LETSENCRYPT_EMAIL=ops@fnbstore.store
          NEXT_PUBLIC_API_BASE_URL=/api/v1
          BACKEND_ENV_FILE=backend/.env.production
          FRONTEND_ENV_FILE=frontend/.env.production
          EOF
          cat <<'EOF' > backend/.env.production
          PORT=4000
          DATABASE_URL=postgresql://fnb_user:StrongPassword1234567890@ci-db-pooler.internal:5432/fnb_stock?schema=public&pgbouncer=true
          DIRECT_URL=postgresql://fnb_user:StrongPassword1234567890@ci-db.internal:5432/fnb_stock?schema=public
          JWT_SECRET=ci-production-jwt-secret-1234567890
          JWT_EXPIRES_IN=1d
          JWT_REFRESH_SECRET=ci-production-refresh-secret-1234567890
          JWT_REFRESH_EXPIRES_IN=7d
          CORS_ORIGIN=https://fnbstore.store
          APP_TIMEZONE=Asia/Ho_Chi_Minh
          TRUST_PROXY=1
          ENABLE_SWAGGER=false
          ENABLE_LOGIN_RATE_LIMIT=true
          LOGIN_RATE_LIMIT_MAX_ATTEMPTS=5
          LOGIN_RATE_LIMIT_WINDOW_MS=600000
          REQUIRE_STRONG_SECRETS=true
          AUTH_COOKIE_NAME=fnb_stock_session
          AUTH_COOKIE_SECURE=true
          AUTH_COOKIE_SAME_SITE=lax
          EOF
          cat <<'EOF' > frontend/.env.production
          NEXT_PUBLIC_API_BASE_URL=/api/v1
          EOF

      - name: Run production preflight
        shell: pwsh
        run: ./deploy/scripts/preflight-check.ps1 -Environment production

      - name: Validate production compose
        run: docker compose --env-file .env.production.compose -f docker-compose.prod.yml config

  build-and-push:
    runs-on: ubuntu-latest
    needs: verify
    if: github.event_name != 'pull_request'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract backend image metadata
        id: backend-meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/fnb-stock-backend
          tags: |
            type=raw,value=latest
            type=sha
            type=ref,event=tag
            type=match,pattern=v(.*),group=1

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile.prod
          target: runner
          push: true
          tags: ${{ steps.backend-meta.outputs.tags }}
          labels: ${{ steps.backend-meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract frontend image metadata
        id: frontend-meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/fnb-stock-frontend
          tags: |
            type=raw,value=latest
            type=sha
            type=ref,event=tag
            type=match,pattern=v(.*),group=1

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile.prod
          push: true
          build-args: |
            NEXT_PUBLIC_API_BASE_URL=/api/v1
          tags: ${{ steps.frontend-meta.outputs.tags }}
          labels: ${{ steps.frontend-meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Create release metadata artifact
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          mkdir -p release-metadata
          cat <<EOF > release-metadata/release-metadata.md
          # Release Metadata

          - Release tag: ${GITHUB_REF_NAME}
          - Commit SHA: ${GITHUB_SHA}
          - Backend image tags:
            ${BACKEND_TAGS}
          - Frontend image tags:
            ${FRONTEND_TAGS}
          - Workflow run: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}
          EOF
        env:
          BACKEND_TAGS: ${{ steps.backend-meta.outputs.tags }}
          FRONTEND_TAGS: ${{ steps.frontend-meta.outputs.tags }}

      - name: Upload release metadata artifact
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@v4
        with:
          name: release-metadata-${{ github.ref_name }}
          path: release-metadata/release-metadata.md
